import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'JobCrack AI - Find the Job. Prepare for the Company. Crack the Interview.',
  description: 'AI-powered career platform for job discovery, resume optimization, company-specific preparation, coding practice, mock interviews and application tracking.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
